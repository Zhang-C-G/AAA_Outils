# 模块修改过程记录：A 快捷字段

最近同步：`2026-04-26`
状态：`active`

## 1. 当前状态

- 核心范围：字段、提示词、快捷字段、A 模块栏目、A 模块悬浮窗
- 当前重点：稳定保存 / 加载、栏目个性化、悬浮窗交互

## 2. 修改记录

### 2026-04-26 / 栏目与悬浮窗收敛
- 改动内容：
  - 选中栏目支持持久化恢复
  - 悬浮窗切为普通悬浮窗逻辑
  - 默认候选切到字段 Top 10
  - 悬浮窗内容列隐藏
  - 快捷字段支持拖拽排序
  - 修正悬浮窗唤出闪动
- 测试：
  - 重启 `main.ahk`
  - `/api/state` 校验 `shortcuts_selected_category`
  - 快捷字段数据链路校验
- 测试结果：`通过`

### 2026-04-26 / 隐藏栏目清空
- 改动内容：
  - 清空隐藏栏目 `prompts` / `quick_fields` 的内容与使用计数
- 测试：
  - `/api/state` 返回 `prompts=0`
  - `/api/state` 返回 `quick_fields=0`
- 测试结果：`通过`

### 2026-04-26 / Top10 来源恢复为 A 模块全量
- 改动内容：
  - A 悬浮窗的默认 Top 10 来源不再只看 `fields`
  - 默认榜单恢复为 A 模块全部条目联合排序
  - 搜索范围也恢复为 A 模块全部条目
- 测试：
  - 代码校验：确认默认榜单遍历 `gCategories`
  - 代码校验：确认搜索不再只限制 `fields`
- 测试结果：`通过`

### 2026-04-26 / 栏目删除确认改为自定义弹窗
- 改动内容：
  - A 模块栏目删除不再使用浏览器默认 `confirm()`
  - 删除确认改为应用内自定义弹窗
  - 删除操作只保留一次确认
- 测试：
  - 代码校验：确认 `deleteTabBtn` 已改为 `confirmDialog`
  - 全局搜索：确认栏目删除链路不再使用默认 `confirm()`
- 测试结果：`通过`

### 2026-04-26 / 悬浮窗唤出闪帧继续收敛
- 改动内容：
  - A 模块悬浮窗新增隐藏态预热，启动时先完成一次离屏布局
  - 每次唤出前先在隐藏态刷新内容、定位和重绘，再一次性显示
  - 收紧首帧重绘时机，减少“先闪一下再出现”的观感
- 测试：
  - 代码校验：确认 `BuildPanelGui()` 已增加隐藏态预热
  - 代码校验：确认 `ShowPanel()` 已改为先 `Show("Hide ...")` 再正式显示
  - 启动校验：通过 `scripts/restart_main_ahk.ps1` 重启原有 `main.ahk` 实例
- 测试结果：`通过`
