# Encoding Policy

## Required encoding
- All docs and scripts must be UTF-8.

## Rules
1. Avoid mixed encodings in same repository.
2. If garbled text appears, rewrite the file in UTF-8 and re-check parser/runtime.
3. Prefer ASCII identifiers for critical parser-sensitive files when possible.
